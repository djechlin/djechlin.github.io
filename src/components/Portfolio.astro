---
import { Github, Linkedin, Mail } from 'lucide-react'

import { Button } from './ui/button'
import Header from './Header.astro'
import Footer from './Footer.astro'
import PostsSection from './PostsSection.astro'
import ProjectCard from './ProjectCard.astro'
import { InterestsSection } from './ui/interests-section'

import projects from '../content/projects.json'
import posts from '../content/posts.json'
---

<div class="flex flex-col min-h-screen bg-background">
  <Header />
  <main class="flex-1">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
      <section class="mb-16">
        <div class="text-center">
          <h1 class="text-4xl font-bold tracking-tight sm:text-5xl mb-4">
            Daniel Echlin
          </h1>
          <p class="text-xl text-gray-600 dark:text-gray-400 mb-8">
            Full-Stack Engineer | Democracy Enthusiast
          </p>
        </div>
      </section>
      <section id="about" class="mb-16">
        <h2 class="text-3xl font-bold mb-4">About Me</h2>
        <div class="space-y-4 text-gray-600 dark:text-gray-400">
          <p>
            I'm a software engineer with experience at Google, MongoDB, and Unroll.me, specializing in building high-scale distributed systems in C++, Java, Python, Go, and Typescript. I prioritize code quality and observability and have a strong track record of mentorship in these areas. As a civic technologist, I've worked in relational organizing and data analysis, helping to pioneer the deep canvassing technique and demonstrating its success in the 2022 Pennsylvania midterm campaigns.
          </p>
        </div>
      </section>
      <section id="projects" class="mb-16">
        <h2 class="text-3xl font-bold mb-8">Projects</h2>
        <div class="flex flex-wrap justify-center gap-6">
          {projects.map((project) => (
            <ProjectCard
            name={project.name}
            description={project.description}
            linkTitle={project.linkTitle}
            url={project.url} />
            ))}
          </div>
        </section>
        <PostsSection
        header="Writing"
        posts={posts}/>
        <InterestsSection />
          <section id="contact" class="mb-16">
            <h2 class="text-3xl font-bold mb-8">Get in Touch</h2>
            <div class="text-center">
              <p class="text-gray-600 dark:text-gray-400 mb-4">
                I&apos;m always open to new opportunities and collaborations. Feel free to reach out!
              </p>
              <div class="flex justify-center space-x-4">
                <a href="https://github.com/djechlin" target="_blank" rel="noopener noreferrer">
                  <Button size="icon" variant="outline">
                    <Github className="h-4 w-4" />
                    <span class="sr-only">GitHub</span>
                  </Button>
                </a>
                <a href="https://www.linkedin.com/in/daniel-echlin-9abb04248/" target="_blank" rel="noopener noreferrer">
                  <Button size="icon" variant="outline">
                    <Linkedin className="h-4 w-4" />
                    <span class="sr-only">LinkedIn</span>
                  </Button>
                </a>
                <a href="mailto:dan773355@danielechlin.xyz">
                  <Button size="icon" variant="outline">
                    <Mail className="h-4 w-4" />
                    <span class="sr-only">Email</span>
                  </Button>
                </a>
              </div>
            </div>
          </section>
        </div>
      </main>
      <Footer />
    </div>